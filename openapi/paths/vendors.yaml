/campaigns/{campaignId}/vendors:
  parameters:
    - $ref: "../openapi.yaml#/components/parameters/CampaignId"
  get:
    operationId: listVendors
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:read]
    summary: >-
      The shops of a campaign. The DM sees every one with all its shelves; a
      player sees only the shops that have been revealed, and within them only
      the lines that have.
    responses:
      "200":
        description: Shops, redacted for the viewer
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "../openapi.yaml#/components/schemas/Vendor"
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"
  post:
    operationId: createVendor
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:run]
    summary: Open a shop (DM only)
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../openapi.yaml#/components/schemas/VendorInput"
    responses:
      "201":
        description: The new shop
        content:
          application/json:
            schema:
              $ref: "../openapi.yaml#/components/schemas/Vendor"
      "400":
        $ref: "../openapi.yaml#/components/responses/BadRequest"
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"

/vendors/{vendorId}:
  parameters:
    - name: vendorId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  patch:
    operationId: updateVendor
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:run]
    summary: Rename a shop, move it, or reveal it to the party (DM only)
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../openapi.yaml#/components/schemas/VendorInput"
    responses:
      "200":
        description: The shop after the change
        content:
          application/json:
            schema:
              $ref: "../openapi.yaml#/components/schemas/Vendor"
      "400":
        $ref: "../openapi.yaml#/components/responses/BadRequest"
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"
      "404":
        $ref: "../openapi.yaml#/components/responses/NotFound"
  delete:
    operationId: deleteVendor
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:run]
    summary: Close a shop and empty its shelves (DM only)
    responses:
      "204":
        description: Closed
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"
      "404":
        $ref: "../openapi.yaml#/components/responses/NotFound"

/vendors/{vendorId}/stock:
  parameters:
    - name: vendorId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  post:
    operationId: addStock
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:run]
    summary: Put something on the shelves (DM only)
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../openapi.yaml#/components/schemas/StockInput"
    responses:
      "201":
        description: The shop, with its new line
        content:
          application/json:
            schema:
              $ref: "../openapi.yaml#/components/schemas/Vendor"
      "400":
        $ref: "../openapi.yaml#/components/responses/BadRequest"
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"
      "404":
        $ref: "../openapi.yaml#/components/responses/NotFound"

/stock/{stockId}:
  parameters:
    - name: stockId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  patch:
    operationId: updateStock
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:run]
    summary: Reprice a line, restock it, or show it to the party (DM only)
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../openapi.yaml#/components/schemas/StockPatch"
    responses:
      "200":
        description: The shop after the change
        content:
          application/json:
            schema:
              $ref: "../openapi.yaml#/components/schemas/Vendor"
      "400":
        $ref: "../openapi.yaml#/components/responses/BadRequest"
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"
      "404":
        $ref: "../openapi.yaml#/components/responses/NotFound"
  delete:
    operationId: deleteStock
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:run]
    summary: Take a line off the shelves (DM only)
    responses:
      "204":
        description: Gone
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"
      "404":
        $ref: "../openapi.yaml#/components/responses/NotFound"

/stock/{stockId}/buy:
  parameters:
    - name: stockId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  post:
    operationId: buyStock
    security:
      - sessionCookie: []
      - bearerToken: [campaigns:play]
    summary: >-
      Buy one of a line (members). Gold only: the price parses to whole gp,
      rounding up in the shopkeeper's favor; the hero must be seated at this
      campaign, and the coin and the goods move together or not at all.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "../openapi.yaml#/components/schemas/BuyRequest"
    responses:
      "200":
        description: The receipt — what was paid, what remains, and the shelf as it stands
        content:
          application/json:
            schema:
              $ref: "../openapi.yaml#/components/schemas/BuyReceipt"
      "400":
        $ref: "../openapi.yaml#/components/responses/BadRequest"
      "401":
        $ref: "../openapi.yaml#/components/responses/Unauthorized"
      "403":
        $ref: "../openapi.yaml#/components/responses/Forbidden"
      "404":
        $ref: "../openapi.yaml#/components/responses/NotFound"
      "409":
        description: Sold out — the shelf emptied under your hand
        content:
          application/json:
            schema:
              $ref: "../openapi.yaml#/components/schemas/Error"
